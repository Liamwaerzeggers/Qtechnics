# Dependencies
node_modules/
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# Testing
/coverage

# Production builds
/build
dist/

# Environment files
# NOTE: .env MOET in git op Emergent — anders deployen secrets/keys niet mee naar productie.
# Emergent past tijdens deploy PROTECTED_VARIABLES (URL/MONGO/DB_NAME) automatisch aan.
# Daarom worden .env bestanden hier NIET genegeerd.

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dump.rdb

# System files
.DS_Store

# Python
__pycache__/
*.pyc
venv/
.venv/

# IDE
.idea/
.vscode/

# Uploads (user data, not in repo)
backend/uploads/

# Build caches
.cache/
frontend/node_modules/.cache/

# Archive files
**/*.zip
**/*.tar.gz
**/*.tar
**/*.tgz
*.pack
*.deb

# Deployment
.vercel

# Misc
chainlit.md
.chainlit
.ipynb_checkpoints/

# Test credentials — never deploy or commit
memory/test_credentials.md

# Credential files (NOT .env — that one must stay in git for Emergent)
credentials.json
*.pem
*.key
.credentials
frontend/node_modules/.cache/default-development/7.pack
